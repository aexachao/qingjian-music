# React Native / Metro 对符号链接的 node_modules 支持不完整，统一用 hoisted 布局
node-linker=hoisted
# 全部缓存与存储放在工作区内，避免写入 HOME
store-dir=.pnpm-store
cache-dir=.cache/pnpm
state-dir=.cache/pnpm-state
strict-peer-dependencies=false
