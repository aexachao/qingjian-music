# ⚠️ 本文件里的 pnpm 专有配置在 pnpm 10+ 下**不会生效**。
#
# 实测 pnpm 11.8.0：本文件声明了 store-dir / cache-dir / state-dir /
# node-linker / strict-peer-dependencies，但 `pnpm config get <key>` 全部返回
# undefined —— 安装照常跑完、不报任何错，设置却是空的。
#
# 这些设置已迁移到 **pnpm-workspace.yaml**（storeDir / cacheDir / stateDir /
# strictPeerDependencies；nodeLinker 目前刻意留空，原因见该文件末尾注释）。
#
# 2026-09-13 之前它们一直写在这里，直接后果就是那个查了很久的
# `EEXIST symlink`：store 落在 ~/Library/pnpm，pnpm 去全局 store 注册项目
# 符号链接时撞上已存在条目。改成在 pnpm-workspace.yaml 里配置后已修复。
#
# 所以：**不要往本文件里加 pnpm 设置**，加在 pnpm-workspace.yaml 里。
# 本文件保留用于 registry / 认证这类 npm 兼容配置。
