# 贡献指南

## 一、先读这段：许可是双轨的

| 发行版 | 许可 | 谁在分发 |
| --- | --- | --- |
| **社区版** | **GPL-3.0-only** | 本仓库；GitHub Actions 产出的自签 IPA / 直装 APK |
| **商店版** | 单独的**商业许可**（不适用 GPL） | App Store 上架版，含永久会员购买 |

社区版会**一直**保持 GPL-3.0。这是本项目对使用者的承诺：任何人都可以自由使用、
修改、再分发，而**衍生物也必须开源** —— 不会出现拿本项目的代码做闭源竞品的情况。

商店版之所以能不受 GPL 约束，唯一的依据是**版权集中在项目所有者手里**：
版权所有者可以给自己另发一份许可。这也意味着，**这套双轨能否继续成立，
取决于项目所有者是否始终持有全部贡献的再许可权**。

## 二、提交 PR 即表示你同意以下授权

请确认你已逐条读过：

1. 你确认自己有权提交这份贡献，且它不侵犯任何第三方的权利；
2. 你授予项目所有者（及其权利受让方）一项**永久的、全球范围的、不可撤销的、免版税的**
   许可，可以**按任意条款**复制、修改、分发、再许可你的贡献，**包括商业与专有条款**；
3. 你**保留**自己贡献的版权。本授权不要求你转让版权，也不限制你把同一份贡献用于其他项目；
4. 你理解你的贡献将随社区版以 GPL-3.0-only 对外发布。

第 2 条是「商店版能走商业许可」的前提。**若你不能接受第 2 条（例如你所在公司对贡献有
额外要求），请先开 issue 讨论，不要直接提 PR** —— 一旦合并就无法回退，
项目会因此永久失去给商店版单独发许可的能力。

## 三、代码约定

改代码前先读 [`docs/build-and-ci.md`](docs/build-and-ci.md)，里面除了构建方式，
还记了一批「不这么写就会坏」的坑。

每次修改完成后跑一次完整验证：

```bash
node scripts/verify-full.mjs
```

它先执行原有的 **JS / TS 校验**（架构守卫 + 文档事实守卫 + ESLint + 4 包类型检查 + 4 包单测；步骤清单见 [`docs/现状基线.md`](docs/现状基线.md)，**本文档不复述项数** —— 那个数字每次加校验步骤都会过期），
然后执行 **SwiftLint 严格检查（必须 0 warning）+ iOS Release 设备版无签名编译 + 产物真实性校验**。
本地 macOS 与 push / PR CI 使用同一套 iOS 验证入口；没有 Xcode 的机器可以先跑
`node scripts/verify.mjs`，但不能据此宣称改动已完成。

几个容易被 CI 拦下的点：

- 新增路由后 `.expo/types/router.d.ts` 不会自动更新，索引 `useSegments()` 一律用 `.at(i)`；
- 纯逻辑必须放 `*-policy.ts`（不能 import RN / expo），否则架构守卫会拦；
- 新增守卫规则时必须同时更新 `scripts/guard-baseline.json` 基线；
- 存量债务是**棘轮**：基线只减不增，新增违规会让 CI 失败。

## 四、修 bug 的 PR 请附一条会失败的测试

本项目对「测试有没有牙齿」比较较真：新增的行为测试，请顺手确认**把它对应的实现改坏时
测试真的会红**（把实现改坏跑一次，再改回来）。永远为真的断言比没有断言更糟。

## 五、第三方补丁

`patches/` 下是 pnpm patch。改完**必须**同步 `pnpm-lock.yaml` 里的哈希
（那是 patch 文件字节的 sha256），否则 `pnpm install --frozen-lockfile` 会直接失败：

```bash
shasum -a 256 patches/*.patch
```

具体原因见 [`docs/build-and-ci.md`](docs/build-and-ci.md) 第四节的第三方补丁条目。
